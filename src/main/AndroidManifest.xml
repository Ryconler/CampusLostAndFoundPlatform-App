<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.dell.campuslostandfoundplatform">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/icon"
        android:label="校园失物招领"
        android:roundIcon="@drawable/icon"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity
            android:name=".MainActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".LoginOrRegisterActivity"
            android:label="校园失物招领"
            android:screenOrientation="portrait"
            android:windowSoftInputMode="adjustUnspecified|stateHidden">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".RegisterActivity"
            android:label="注册"
            android:screenOrientation="portrait"
            android:windowSoftInputMode="adjustUnspecified|stateHidden" />
        <activity
            android:name=".GetSchoolActivity"
            android:label="选择学校"
            android:screenOrientation="portrait" />
        <activity
            android:name=".PublishLostActivity"
            android:windowSoftInputMode="adjustUnspecified|stateHidden" />
        <activity android:name=".ArticleDetailActivity" />
        <activity android:name=".MyPublishLostActivity" />
        <activity android:name=".MyPublishFoundActivity" />
        <activity android:name=".MyInfoActivity" />
        <activity android:name=".AboutUsActivity" />
        <activity
            android:name=".PublishFoundActivity"
            android:windowSoftInputMode="adjustUnspecified|stateHidden" />
        <activity android:name=".MyRecevieThank"></activity>
    </application>

</manifest>